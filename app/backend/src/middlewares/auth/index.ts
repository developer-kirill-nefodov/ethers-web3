import {loginMiddleware} from "./login.middlewares";
import {forgotPasswordMiddleware} from "./forgot-password.middlewares";
import {resetPasswordMiddleware} from "./reset-password.middlewares";

export {
  loginMiddleware,
  forgotPasswordMiddleware,
  resetPasswordMiddleware
}
